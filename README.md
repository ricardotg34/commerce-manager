# Commerce Manager

## Install

1. Copy .env.example file content to .env file and add or modify variables if it is needed.
2. Run `docker-compose up -d` to run go application and postgres database (Install docker if needee).
3. In order to watch the app documentation, go to `/swagger/index.html` path.
4. You may want to add a user first with the GET /user endpoint in order to use some of the other endpoints